# Rescuebot

Variable names and functions that have descriptive names have all been named based on context clues.  They are best-guesses, and may not be accurate.

- [rescuebot_main.c](rescuebot_main.c) contains raw decompiled code of what appears to be Rescuebot's main function.

- [find_assign_victim.c](find_assign_victim.c) contains what appears to control how victims are found/assigned

- [assign_victim_to_bot.c](assign_victim_to_bot.c) contains what appears to assign a victim to a bot


## AI Slop

Below was generated by feeding Claude all context clues and asking for further its findings.  It appears to largely be true, but should not be focused on too heavily as there may be hallucinations.

### Variables


RescueBot State Struct

| Offset | Size | Type | Resolved Type | Name |
|---------|------|------|---------------|------|
| 0x0     | 0x4  | int *  | int *  | current_location_id_or_dropoff_point_l |
| 0x4     | 0x4  | uint   | uint   | has_victim_l |
| 0x8     | 0x1  | ??     | undefined |  |
| 0x9     | 0x1  | ??     | undefined |  |
| 0xa     | 0x1  | ??     | undefined |  |
| 0xb     | 0x1  | ??     | undefined |  |
| 0xc     | 0x4  | uint   | uint   | message_data_ptr_l |
| 0x10    | 0x4  | int *  | int *  | message_timer_l |
| 0x14    | 0x4  | int *  | int *  | rescue_attempt_counter_l |
| 0x18    | 0x4  | uint   | uint   | is_active_l |

### Function Structure

The function operates in three main sections, all gated by an initial is_active check:

**Section 1: Victim Drop-off Logic**
- Trigger Condition: Bot is carrying a victim AND at valid drop-off location
- Calls drop-off function to release the victim
- Logs "RESCUEBOT: Victim dropped off"
- Clears the has_victim flag
- Updates bot state and display

**Section 2a: Victim Pickup/Rescue Initiation**
- Trigger Condition: Bot is NOT carrying a victim AND game state allows rescues (states 3, 4, or 5)
- This section has multiple validation checks:
- Debug Override Check
- If debug mode active (DAT_00502a78), checks for "DBG_ACTIVATE_RESCUEBOT" flag
- If flag present, increments rescue_attempt_counter to potentially skip normal activation limits

**Section 2b: Rescue Attempt Validation**
- Verifies rescue_attempt_counter is below threshold (DAT_00502afc)
- Sets up coordinate array (local_80[1-6]) with target/spawn values
- Retrieves maximum allowed victims from FUN_004b9400()
- Checks if current_victim_count is below max limit
- If valid: Calls FUN_00455230() to initiate rescue
- If exceeded: Logs error "RESCUEBOT: Exceeded maximum of X victims"

**Section 2c: State Finalization**
- Calls FUN_004541a0() to finalize rescue attempt state

**Section 3: Message Display System**
- Trigger Condition: message_timer is below activation threshold

- Initializes difficulty settings (singleton pattern) if not already initialized
- Retrieves message data using hash lookup
- Formats message with colored text (^(128,128,240) cyan, ^(255,255,255) white)
- Displays formatted message to game UI
- Updates bot state after display

**Game World State Struct**

- game_or_level_state (0x18c) - Game state enum (3,4,5 = active gameplay, 6 = special/disabled)

- current_victim_count (0x3530) - Total active victims in level, checked against max limit

