notes from talking to dav

nydesson uses igt

pauze's checks gamestate and includes newspaper at end

rta currently includes loading

we need
    livesplit timer that can reliably time records by the frame
    between first frame of actionability within level
    and hitting end trigger

starting timer will be more dificult
    can i look for when the main game loop is active after a load?
    we can also maybe use pauze's trigger for start, if this is difficult



if there is are exit and rescue triggers, exit trigger is the exit point

some levels only have exit triggers

bank has feedback loop where if you touch exit trigger before rescuing

boat has multiple maps, as one level
3 levels are segmented
    each of them has an end trigger, but not all of them call credits
    rescue triggers and end triggers are different

Strings to search for
    level begins
    Find and rescue the survivors!

    more survivors
    "You can't leave now!  There are still survivors in:
        boat
            cargo a
            engine room

rescuebot
        blade jackson
        hotrod stein
        spitfire li
        smockestack williams
        brushfire miller
        

f8 

survivor subsplits for IL's


